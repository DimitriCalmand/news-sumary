version: '3.8'

services:
  backend:
    build: ./backend
    networks:
      - app-network

  frontend:
    build: ./frontend
    ports:
      - "3000:8000"  # Mappe le port 3000 de l'hôte au port 8000 du conteneur frontend
    networks:
      - app-network
    depends_on:
      - backend

networks:
  app-network:
    driver: bridge